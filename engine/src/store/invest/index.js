/**
* 此actions涉及invest(调查)模块下的数据；
 * 创建者 ycl
 * 时间 2020-01-14
* */
import state from './state';
import getters from './getters';
import mutations from './mutations';
import actions from './actions';

export default {
  state,
  getters,
  mutations,
  actions
}

